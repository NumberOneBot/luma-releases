### Fixed

- Update check now correctly points to the `luma-releases` distribution repository
- Content Security Policy updated to allow `api.github.com` connections

### Added

- Update available dialog вЂ” shown automatically on launch (when auto-check is enabled) and on manual check from Settings; dismissible with "Later"
- Error state in Settings update section when the update check fails
- Per-version changelog files published to the releases repository
- Auto-generated GitHub Release notes from `CHANGELOG.md`
